# Node.js
# ========================
# Ignore all node_modules directories
node_modules/
# Ignore package-lock.json and yarn.lock, which are re-generated by npm/yarn install
package-lock.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm/
.yarn-cache/
.pnpm-store/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS and Editor Specific
# ========================
.DS_Store # macOS
.vscode/ # VS Code settings, if you don't want to share them
.idea/ # IntelliJ IDEA settings
*.sublime-project # Sublime Text projects
*.sublime-workspace # Sublime Text workspaces
*.bak # Backup files
*.swp # Vim swap files
*~ # Emacs backup files
Thumbs.db # Windows thumbnails
ehthumbs.db # Windows media thumbnails
Desktop.ini # Windows desktop settings
.directory # Linux directory settings

# Environment variables
# ========================
.env # Local environment variables (sensitive data)
.env.*.local # Vite's local env files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Server specific (if any build artifacts or local data are generated)
# ========================
# No specific build output for Node.js server itself usually,
# but if you have a /dist or /build folder for server-side transpilation
# you would add it here, e.g.:
# server/dist/
# server/build/

# Client specific (Vite/React build output, managed by client/.gitignore too, but good to have here)
# ========================
# The 'dist' folder is where Vite puts the production build for the client.
client/dist/

# Other temporary files
# ========================
coverage/
.nyc_output/
.cache/
.eslintcache